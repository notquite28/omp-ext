{
  "name": "omp-grok-build-marketplace",
  "version": "0.2.0",
  "private": true,
  "description": "Marketplace repo for omp-grok-build and omp-rewind plugins",
  "type": "module",
  "scripts": {
    "test": "bun run test:grok && bun run test:rewind",
    "test:grok": "bun --cwd plugins/omp-grok-build test",
    "test:rewind": "bun --cwd plugins/omp-rewind tests/core.test.ts",
    "typecheck": "bun --cwd plugins/omp-grok-build run typecheck",
    "install:all": "bun install --cwd plugins/omp-grok-build"
  }
}
